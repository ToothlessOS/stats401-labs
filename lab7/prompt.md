I am designing the visualizations for lab 7, please help me with the implementation:

STAGE 1 (Completed):

1. The current logic for determining the active links work. For the encoding, my idea is to put the companies that are in the same region (i.e. north America, Asia ...) together and circled with dashed lines (need to modify the force simulation for that). The colors should encode the industry of the different companies instead. Add the company names onto the network as well.
2. Add the legend (which is empty) now and finish the link tooltip.
3. Add controllable animation with "Play", "Pause", "Reset", "Time slider", "Current day/date", attach to the "controls" div in index.html.

STAGE 2:
1. 